package voya401k;

/**
 * Different types of intents that could be sent from the virtual assistant.
 */
public enum VoyaIntentType {
    YES, NO, SUMMARY, QUIT, PIN, SAVINGSRATE, RATEOFRETURN, BALANCE, RETIREMENTAGE, ACCOUNTACTIVITY
}
